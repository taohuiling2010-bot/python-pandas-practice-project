# 豆瓣电影 Top250 数据分析项目

## 一、项目概述

- **项目周期**：2026.02 - 2026.03
- **项目角色**：独立负责人（数据采集、清洗、分析、可视化全流程）
- **核心技术栈**：Python（requests / lxml / Pandas / NumPy / Matplotlib）、Excel、Jupyter Notebook
- **数据规模**：250 部电影、9 个核心字段、共 2238 条数据点

## 二、项目背景

本项目作为**数据分析能力练习项目**，通过对豆瓣电影 Top250 的端到端分析，演示
"网络数据采集 → 数据清洗 → 统计分析 → 可视化呈现"的完整工作流。

数据题材选择豆瓣电影榜单是出于**数据获取的合规性**与**字段维度的丰富性**考虑——
本项目演示的所有数据处理方法（爬虫、清洗、独热编码、相关性分析、可视化）均可迁移
至任何业务场景的数据分析工作，包括财务数据分析、零售运营分析、客户行为分析等。

## 三、核心工作

### 3.1 数据采集（Python 爬虫）

- 使用 `requests` 库发送 HTTP 请求，结合反爬策略（User-Agent 伪装、Referer、请求间隔）
- 使用 `lxml.etree` 解析 HTML，通过 XPath 定位提取目标字段
- 跨 10 页循环爬取 250 部电影的 9 个字段：排名、电影名、评分、评价人数、导演、年份、国家、类型、主演
- 使用 `openpyxl` 将爬取数据格式化存储为 Excel

### 3.2 数据清洗（Pandas）

- **缺失值处理**：识别"主演"字段的缺失记录并定位处理
- **字段标准化**：
  - 年份字段用正则 `r'(\d{4})'` 从混合字符串中提取 4 位数字年份
  - 导演/演员字段用正则 `r'[\u4e00-\u9fff·]+'` 提取中文部分，剔除英文与特殊字符
- **数据格式转换**：将年份字符串转为数值型，评分字段类型规范化
- **数据结构整理**：删除原始杂乱字段，重命名清洗后的标准化字段

### 3.3 统计分析与可视化

通过五大业务问题驱动分析：

| 编号 | 业务问题 | 分析方法 | 可视化 |
|---|---|---|---|
| Q1 | 什么类型的电影上榜最多？ | 独热编码 + 计数排序 | 饼图、柱状图 |
| Q2 | 哪些年份的电影上榜最多？ | 时间序列分布分析 | 折线图、柱状图 |
| Q3 | 哪些国家/地区上榜最多？ | 独热编码 + TOP10 排名 | 柱状图 |
| Q4 | 上榜最多的导演和演员？ | 字段拆分 + 频次统计 | 柱状图 |
| Q5 | 评分和评价人数的关系？ | 相关系数 + 散点图 | 散点图 |

**技术亮点**：
- 自定义函数 `field_sum(field)` 封装"独热编码 + 频次统计"逻辑，实现可复用代码工程化
- 同一分析任务对比使用 matplotlib 原生绘图与 pandas 内置绘图，掌握不同库的取舍
- 使用 `corr()` 计算评分与评价人数的相关系数，从相关性角度量化指标关系

## 四、核心成果

1. **完整工作流闭环**：实现从"网页 HTML → 结构化 Excel → 清洗后的标准数据 → 多维度分析 → 可视化报告"的端到端数据分析流程
2. **技术能力掌握**：熟练运用 Python 数据分析全家桶（Pandas、NumPy、Matplotlib），具备非结构化数据处理与正则清洗的实操能力
3. **业务问题驱动**：以五大具体业务问题为驱动，避免"为了用工具而用工具"的低效练习模式
4. **可迁移性强**：项目所用的爬虫框架、数据清洗逻辑、独热编码分析方法均可直接迁移至业务场景

## 五、项目反思与局限

- **数据集规模有限**：250 条数据足够练习，但不足以做更深入的统计推断或机器学习
- **分析维度可扩展**：未来可加入 NLP 处理影片简介、情感分析评论数据等更高阶的分析
- **可视化层次可提升**：当前使用 Matplotlib 静态图，未来可尝试 Seaborn 美化或 Plotly 交互式可视化

## 六、相关文件

- `01_爬虫代码.py` — 网页数据爬取脚本
- `02_数据分析代码.ipynb` — 数据清洗、分析与可视化全流程
- `03_数据/豆瓣Top250数据.xlsx` — 爬取后的结构化数据
- `04_可视化分析报告.pdf` — 分析结果可视化输出